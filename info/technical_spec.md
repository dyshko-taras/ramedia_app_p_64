## Screen: **Preloader / Splash Screen**

### Purpose

Екран призначений для початкового запуску додатку.
Використовується для:

* ініціалізації застосунку;
* підготовки локальних даних;
* перевірки стану користувача (авторизований / ні);
* плавного входу в основний flow додатку.

---

### UI Elements

* **Background image**

  * Статична фонова картинка (згідно дизайну).
  * Заповнює весь екран (Full Screen, aspect fill).
- **Lottie Animation (Loader)**
    - Анімований loader, реалізований через **Lottie**.
    - Відображається поверх фонової картинки.
    - Розміщення:
        - по центру екрану **або**
        - у зоні, визначеній дизайнером.
    - Анімація:
        - зациклена (infinite loop);
        - без текстового супроводу;
        - відтворюється автоматично при відкритті екрана.
    - Анімація зупиняється та екран закривається після завершення ініціалізації.
---

### User Actions

* Користувач **не має можливості взаємодії** з екраном.
* Всі дії виконуються автоматично.

---

### Business Logic

1. Після запуску додатку відображається Preloader.
2. Виконується початкова ініціалізація:

   * перевірка збереженого стану (local storage);
   * перевірка, чи існує сімейна група;
   * перевірка, чи користувач вже створений у системі.
3. Після завершення ініціалізації:

   * автоматичний перехід на наступний екран.

---

### Navigation

* **If user is new / not initialized**

  * → Onboarding / Create Family Group (екран буде описаний пізніше).
* **If user already exists**

  * → Main App Flow (Dashboard / Family Bank).

Навігація виконується **без можливості повернення назад**.

---

### States

* **Loading (default)**

  * Відображається фон + loader.
* **Error (optional, якщо потрібно)**

  * У випадку критичної помилки:

    * повторна спроба ініціалізації;
    * або fallback-перехід.

---

### Technical Notes

* Екран не має власного state management, окрім lifecycle.
* Час відображення — залежить від швидкості ініціалізації, мінімум 1–2 секунди (опційно, для UX).
* Не повинен мигати при повторному відкритті додатку з background.
* Додаток використовує тільки локальні дані

---

## Screen: **Onboarding – Step 1**

### Purpose

Перший екран онбордингу знайомить користувача з ідеєю додатку **Home Credit** та пояснює основну цінність — спільне ведення домашнього бюджету.
Також екран використовується для першого запиту дозволу на **push notifications**.

---

### UI Elements

* **Background image**

  * Статична фонова картинка.
  * Заповнює весь екран (Full Screen).
  * Є частиною брендового стилю онбордингу.

* **Foreground image (PNG)**

  * PNG-зображення людей.
  * Розташоване в нижній частині екрана.
  * Накладається поверх фонового зображення.
  * Не інтерактивне.

* **Title Text**

  * Основний заголовок (великий, жирний текст).
  * Приклад:

    > *HOME CREDIT: WE MANAGE THE HOME BUDGET TOGETHER*
  * Центрований або вирівняний згідно дизайну.

* **Description Text**

  * Пояснювальний текст під заголовком.
  * Коротко описує функціональність додатку.
  * Приклад:

    > *Our application will help you split family expenses together!*

* **Primary Button**

  * Кнопка **Continue**.
  * Основна CTA-кнопка екрану.
  * Активна за замовчуванням.

---

### User Actions

* Натискання на кнопку **Continue**.

---

### Business Logic

1. При першому відкритті екрану:

   * автоматично або після натискання **Continue** ініціюється запит на дозвіл **Push Notifications**.
2. Поведінка після запиту дозволу:

   * **Allow** → дозвіл зберігається в системі.
   * **Deny** → додаток продовжує роботу без нотифікацій.
3. Вибір користувача **не блокує** подальший flow онбордингу.
4. Після обробки результату (allow / deny):

   * перехід на **Onboarding – Step 2**.

---

### Notifications Permission

* Тип: **System permission dialog**
* Платформи:

  * iOS — стандартний system alert.
  * Android — відповідний permission flow.
* Запит виконується **тільки один раз**.
* Повторний запит можливий лише з налаштувань додатку.

---

### Navigation

* **Continue**

  * → Onboarding – Step 2
* Повернення назад з цього екрану **не передбачене**.

---

### States

* **Default**

  * Відображається контент екрану.
* **Permission Requested**

  * Системний діалог поверх екрану.
* **Post-permission**

  * Автоматичний перехід далі.

---

### Edge Cases

* Користувач закрив додаток під час permission dialog:

  * при повторному відкритті онбординг продовжується.
* Відсутність інтернету:

  * не впливає на роботу екрану.
* Permission already granted / denied раніше:

  * системний діалог не показується;
  * одразу виконується перехід на наступний екран.

---

### Technical Notes

* Permission status зберігається локально.
* Екран є частиною **Onboarding flow** і не відображається після його завершення.

---


## Screen: **Onboarding – Step 2 (Add Members & Currency)**

### Purpose

Другий екран онбордингу призначений для:

* додавання учасників сімейної групи;
* задання базової валюти сімейного бюджету;
* збереження початкових даних перед переходом у основний функціонал додатку.

Екран є ключовим для формування початкової структури сімейного банку.

---

### UI Elements

#### Background

* **Solid green background**
* Без фонового зображення.
* Єдиний стиль з іншими екранами онбордингу.

---

#### Text Elements

* **Title**

  * `ADD ALL MEMBERS RIGHT NOW`
* **Subtitle**

  * `Enter your name and photo`
* **Helper Text**

  * Пояснення про додавання учасників та ліміт:

    * максимум **10 учасників**.

---

#### Members List

* Список доданих учасників.
* Кожен елемент містить:

  * avatar (круглий);
  * ім’я учасника.
* Список динамічний, оновлюється після кожного додавання / видалення.

---

#### Add Member Button

* Кнопка **“Add a team member”**
* Відображається:

  * поки кількість учасників < 10.
* Після досягнення 10 учасників:

  * кнопка **приховується**.

---

#### Currency Selector

* **Dropdown / Select**
* Мінімальний список валют (наприклад):

  * USD
  * EUR
  * UAH
* Обрана валюта використовується як **базова** для всього додатку.

---

#### Bottom Buttons

* **Skip**

  * Secondary button.
* **Continue**

  * Primary button.

---

### User Actions

* Натиснути **Add a team member**
* Додати ім’я учасника
* Додати / змінити фото учасника
* Видалити учасника
* Вибрати валюту
* Натиснути **Skip**
* Натиснути **Continue**

---

## Modal: **Add / Edit Member (showModalBottomSheet)**

### Purpose

Модальне вікно для створення або редагування учасника.

---

### UI Elements (Modal)

* **Title**

  * `Add your name`

* **Close icon (X)**

  * Закриває модал без збереження.

* **Text Input**

  * Placeholder: `Participant name`
  * Обовʼязкове поле.

* **Photo Picker**

  * Кнопка `+`
  * Вибір фото з галереї пристрою.
  * Фото відображається як preview після вибору.

* **Buttons**

  * **Save**

    * Активна лише якщо введене імʼя.
  * **Clear information**

    * Очищає імʼя та фото.

---

### Modal Business Logic

* Фото зберігається локально (path / base64).
* При натисканні **Save**:

  * учасник додається або оновлюється у списку.
* При натисканні **Clear information**:

  * поля очищуються.
* Закриття модалки без Save → зміни не зберігаються.

---

## Delete Participant Flow

### Trigger

* Довгий тап або окрема дія по учаснику.

### Confirmation Dialog

* Title: `Delete Participant`
* Text: `Do you want to delete your data?`
* Buttons:

  * **Delete**
  * **Cancel**

### Logic

* **Delete**

  * учасник видаляється зі списку;
  * дані оновлюються у сховищі.
* **Cancel**

  * без змін.

---

### Business Logic (Screen)

* Максимальна кількість учасників: **10**
* Мінімальна кількість:

  * 1 учасник (поточний користувач).
* При додаванні нового учасника:

  * кнопка **Add a team member** залишається доступною, доки не досягнуто ліміт.
* Валюта:

  * обовʼязкова;
  * має дефолтне значення (наприклад USD).

---

### Navigation

* **Skip**

  * → **Calculate Credit**
  * Дані учасників не обовʼязкові.
* **Continue**

  * → **Calculate Credit**
  * Зі збереженням усіх введених даних.
* Повернення назад у Onboarding Step 1 **не передбачене**.

---

### Data Storage

* Тип сховища: **SharedPreferences**
* Формат учасників: **JSON**

#### Example: Participants JSON

```json
[
  {
    "id": "uuid",
    "name": "Nastya",
    "avatar": "local_path_or_base64"
  },
  {
    "id": "uuid",
    "name": "Katya",
    "avatar": "local_path_or_base64"
  }
]
```

#### Stored Keys

* `participants_list`
* `selected_currency`
* `onboarding_completed`

---

### States

* **Empty**

  * немає учасників.
* **Partially filled**

  * 1–9 учасників.
* **Max filled**

  * 10 учасників, кнопка додавання прихована.

---

### Edge Cases

* Користувач закрив додаток під час додавання учасника:

  * незбережені дані губляться.
* Відмова в доступі до галереї:

  * фото не додається, можна зберегти лише імʼя.
* SharedPreferences очищено:

  * онбординг проходиться заново.

---

### Technical Notes

* Усі дані онбордингу зберігаються локально.
* Дані використовуються для ініціалізації сімейного банку при першому вході.

---

Ага, супер, тоді робимо **чисто технічний опис Bottom Bar як контейнера**, **без опису внутрішніх екранів** 👍
Ось правильна версія **technical_spec** саме в тому вигляді, як ти задумував.

---

## Component: **Bottom Navigation Bar (Main App Layout)**

### Purpose

Bottom Navigation Bar є основним навігаційним контейнером додатку після завершення Onboarding.
Компонент відповідає **лише за перемикання між root-екранами** за допомогою `IndexedStack`, без опису логіки самих екранів.

---

### Structure

* Основний layout складається з:

  * **IndexedStack**
  * **Bottom Navigation Bar**

* Кількість табів: **3**

  1. Home
  2. Credit
  3. Settings

---

### Navigation Logic

* Перемикання табів відбувається через **IndexedStack**.

* Кожен таб:

  * ініціалізується один раз;
  * зберігає свій state при перемиканні;
  * не перезавантажується повторно.

* Активний індекс зберігається в локальному state layoutʼу.

---

### UI Elements

#### Bottom Bar Container

* Розташований фіксовано внизу екрану.
* Фон: зелений (згідно дизайну).
* Завжди видимий у main flow додатку.

---

#### Tab Item

Кожен таб містить:

* Іконку
* Текстовий лейбл

**States:**

* Active
* Inactive

---

### User Actions

* Тап по табу:

  * змінює `currentIndex`;
  * оновлює активний стан іконки та тексту;
  * відображає відповідний екран у `IndexedStack`.

---

### State Management

* `currentIndex` зберігається:

  * у State widgetʼі або
  * у глобальному state (якщо буде використано).
* Перемикання між табами:

  * не очищає state вкладених екранів;
  * не викликає повторну ініціалізацію.

---

### Visibility Rules

Bottom Navigation Bar **не відображається**:

* на Preloader;
* під час Onboarding;
* поверх system dialogs;
* поверх full-screen modal screens (bottom sheets).

---

### Technical Notes

* Bottom bar є **root layout** додатку.
* Навігація всередині кожного табу:

  * не впливає на інші таби;
  * не змінює `currentIndex`.
* Компонент не містить бізнес-логіки.

---

### Edge Cases

* Повторний тап по активному табу:

  * не виконує жодних дій.
* Швидке перемикання між табами:

  * не викликає race conditions.
* Вихід з додатку:

  * індекс може бути відновлений або скинутий (за рішенням).

---

### Implementation Constraints

* Заборонено використовувати push-навігацію між табами.
* Дозволено лише керування індексом `IndexedStack`.

---


## Screen: **Home**

### Purpose

Home — основний екран додатку, який:

* відображає **загальний баланс сімейного бюджету**;
* показує **історію транзакцій** (доходи / витрати);
* є точкою входу для:

  * додавання доходів;
  * додавання витрат;
  * перегляду балансу учасників.

---

## UI Structure (High-level)

### Header Section

* Avatar поточного користувача
* Text:

  * `Welcome!`
  * `{User name}`
* **Total budget amount**

  * Велике число (загальний Family Bank баланс)
* Button: **Details**

  * Веде на екран **Participants' Balance**

---

### Quick Actions Panel

Контейнер з 3 кнопками:

1. **Add income**
2. **Add expenses**
3. **Add participant**

---

### Transactions List

* Список усіх транзакцій:

  * доходи;
  * витрати.
* Кожен елемент:

  * avatar учасника;
  * імʼя;
  * дата;
  * сума (+ / −).
* Список відсортований за датою (нові зверху).

---

### Empty State

Якщо транзакцій немає:

* Text:

  > No new records have been entered yet, you can create a new record.

---

## User Actions (Home)

* Tap **Add income**
* Tap **Add expenses**
* Tap **Add participant**
* Tap **Details**
* Tap / long tap на транзакцію

---

## Modal: **Add Income (showModalBottomSheet)**

### Purpose

Додавання надходження до сімейного банку.

---

### UI Elements

* Title: `Add Income`
* Close (X)

#### Fields

1. **Who is topping up**

   * Якщо учасники існують:

     * список chips з іменами.
   * Якщо учасників немає:

     * кнопка **Add participant** → редирект в Onboarding додавання.

2. **Amount**

   * Numeric input
   * Currency selector (dropdown)

3. **Payment schedule, date**

   * Date picker
   * Default: `Today`

4. **Save button**

   * Disabled за замовчуванням
   * Активна коли:

     * вибрано учасника;
     * введено суму.

---

### Business Logic (Income)

* Надходження:

  * належить **одному** учаснику;
  * збільшує Family Bank balance;
  * збільшує баланс конкретного учасника.
* Після Save:

  * транзакція додається в список;
  * Home оновлюється.

---

## Modal: **Add Expenses (showModalBottomSheet)**

### Purpose

Додавання витрати з можливістю розподілу між учасниками та створення боргу.

---

### UI Elements

* Title: `Add Expenses`
* Close (X)

#### Fields

1. **Who is spending**

   * Chips зі списком учасників
   * Якщо немає учасників:

     * кнопка **Add participant** → онбординг

2. **Amount**

   * Numeric input
   * Currency selector

3. **Split transaction?**

   * Radio buttons:

     * Yes
     * No

4. **Split percentage**

   * Зʼявляється якщо `Yes`
   * Input % (тільки числа)

5. **Payment schedule, date**

   * Date picker
   * Default: Today

6. **Save**

   * Активна лише коли всі обовʼязкові поля заповнені

---

### Business Logic (Expenses)

#### Без розподілу (Split = No)

* Витрата:

  * списується з балансу учасника;
  * зменшує Family Bank balance.

#### З розподілом (Split = Yes)

* Користувач обирає:

  * учасників;
  * відсоток.
* Сума ділиться відповідно до %.
* Якщо у платника **недостатньо коштів**:

  * система пропонує **взяти борг** в іншого учасника;
  * борг створюється автоматично.

---

### Borrow Logic

* Якщо баланс < суми витрати:

  * показується повідомлення:

    > You have exceeded your balance by $X
* Доступні учасники для позики:

  * ті, у кого позитивний баланс.
* Якщо таких немає:

  * показується dialog:

    > Enter a different amount

---

## Screen: **Participants' Balance**

### Purpose

Екран для перегляду:

* балансу кожного учасника;
* активних боргів (хто кому винен).

---

### UI Elements

* Header: `Participants' Balance`
* List of participants:

  * avatar
  * name
  * balance
* Debts section:

  * `{User A} owes {User B} $X`

---

### Business Logic

* Баланс учасника:

  * доходи − витрати − борги.
* Якщо борг погашається через дохід:

  * борг закривається автоматично;
  * сума списується як витрата;
  * додається як дохід кредитору.

---

## Transactions Interaction

### Long Tap on Transaction

* Context menu:

  * Edit
  * Delete

### Delete

* Confirmation dialog:

  * `Delete this entry?`
* Після підтвердження:

  * транзакція видаляється;
  * всі баланси перераховуються.

---

## Data Storage (Home)

* Усі операції зберігаються в **SharedPreferences**:

  * transactions (JSON);
  * participants balances;
  * debts.

---

## Edge Cases

* Видалення транзакції з боргом:

  * борг також видаляється.
* Редагування транзакції:

  * відкривається відповідний modal;
  * після Save перерахунок балансу.
* Валюта транзакції ≠ базова:

  * на цьому етапі без конвертації (якщо не зазначено інакше).

---

## Screen: **Credit (Credit Calculator)**

### Purpose

Екран **Credit** призначений для:

* розрахунку кредиту / позики;
* моделювання умов кредиту (сума, термін, відсоток);
* відображення комісії, відсотків та загальної суми погашення.

Екран **не створює реальну транзакцію**, а використовується як **калькулятор / моделювання**.

---

## UI Structure

### Header

* Title: **Credit Calculator**
* Стандартний app bar без додаткових дій.

---

### Summary Card (Top Block)

Картка з результатами розрахунку.

#### Fields:

* **Credit Amount**

  * Основна сума кредиту.
* **Interest**

  * Загальна сума відсотків.
* **Repayment Amount**

  * Повна сума до погашення (Credit Amount + Interest).

👉 Значення:

* За замовчуванням `0`.
* Оновлюються після натискання **Calculate**.

---

### Sliders Section

#### 1. Loan Amount (USD)

* Slider:

  * Мін: `100 USD`
  * Макс: `100 000 USD`
* Поточне значення відображається справа.
* Значення змінюється в реальному часі при русі слайдера.

---

#### 2. Loan Term (months)

* Slider:

  * Мін: `1`
  * Макс: `24`
* Відображає кількість місяців.
* Ціле число.

---

#### 3. Interest Rate (%)

* Slider:

  * Мін: `0%`
  * Макс: `10%`
* Крок: за рішенням (наприклад 0.1%).

---

### Calculate Button

* Button: **Calculate**
* Disabled за замовчуванням.
* Стає активною коли:

  * Loan Amount > 0
  * Loan Term > 0
  * Interest Rate ≥ 0

---

## User Actions

* Пересування будь-якого слайдера
* Натискання **Calculate**

---

## Business Logic

### Input Logic

* Усі значення вводяться **тільки через sliders**.
* Ручний текстовий ввід не передбачений.
* При зміні будь-якого параметра:

  * попередній результат скидається (Interest = 0, Repayment = 0).

---

### Calculation Logic

#### Interest Calculation

```
Interest = LoanAmount × (InterestRate / 100) × (LoanTerm / 12)
```

#### Repayment Amount

```
RepaymentAmount = LoanAmount + Interest
```

> Формула може бути замінена на іншу (анюїтет / прості відсотки) без зміни UI.

---

### Result Update Flow

1. Користувач налаштовує параметри.
2. Натискає **Calculate**.
3. Вираховується:

   * сума відсотків;
   * загальна сума погашення.
4. Дані відображаються у Summary Card.

---

## States

### Default

* Loan Amount = 0
* Interest = 0
* Repayment = 0
* Button disabled

---

### Editing

* Користувач рухає sliders.
* Button активна.
* Results ще не пораховані.

---

### Calculated

* Results відображені.
* Значення зафіксовані до наступної зміни sliderʼів.

---

## Navigation

* Екран відкривається через **Bottom Navigation Bar → Credit**.
* Bottom bar **залишається видимим**.
* Немає push-навігації з цього екрану.

---

## Data Persistence

* Дані **не зберігаються**:

  * ні в SharedPreferences;
  * ні в історії.
* При повторному відкритті табу:

  * стан може бути скинутий або збережений (за рішенням).

---

## Edge Cases

* Interest Rate = 0%:

  * Interest = 0;
  * Repayment = LoanAmount.
* Мінімальні значення:

  * Calculate не виконується при Loan Amount = 0.
* Дуже великі значення:

  * округлення до 2 знаків після коми.

---

## Technical Notes

* Екран ізольований від Home / Credit logic.
* Не впливає на:

  * Family Bank;
  * учасників;
  * борги.
* Реалізується як **pure UI + calculation logic**.

---

## Screen: **Settings**

### Purpose

Екран **Settings** призначений для:

* керування профілем користувача;
* переходу до системних налаштувань нотифікацій;
* перегляду юридичної інформації;
* поширення застосунку;
* повного очищення локальних даних додатку.

---

## UI Structure

### Header

* Title: **Settings**
* Standard app bar.
* Bottom Navigation Bar — **visible**.

---

### Profile Section

#### UI

* Avatar користувача

* User name

* Subtitle:

  * `Since {date}`

* Edit icon (✏️)

#### User Actions

* Tap on profile section → відкриває **Edit Profile Modal**

---

## Modal: Edit Profile - це той самий Add your name

## Settings List Items

### 1. Notifications

#### UI

* List item з arrow →

#### Behavior

1. **First tap**:

   * якщо permission ще не запитувався:

     * показується system permission dialog.
2. **Subsequent taps**:

   * відкриває **system notification settings** додатку.

> Якщо permission відхилений — одразу відкривається system settings.

---

### 2. Privacy Policy

#### UI

* List item з arrow →

#### Behavior

* Відкриває браузер через `url_launcher`.

```dart
launchUrl(Uri.parse("{PRIVACY_LINK}"));
```

---

### 3. Share Application

#### UI

* List item з arrow →

#### Behavior

* Відкриває system share sheet через `share_plus`.

```dart
Share.share("{APPSTORE_LINK}");
```

---

### 4. Clear Data

#### UI

* List item з arrow →
* Критична дія.

---

### Clear Data Flow

#### Confirmation Dialog

* Title: **Clear data**
* Text: `Do you want to clear your data?`
* Buttons:

  * **Accept**
  * **Cancel**

#### Business Logic

* **Accept**

  * повністю очищає SharedPreferences:

    * користувач;
    * учасники;
    * транзакції;
    * борги;
    * налаштування.
  * редирект у **Onboarding**.
* **Cancel**

  * діалог закривається.

---

## Empty / Error States

### No Internet Connection

* Full-screen fallback screen.
* Text:

  > It seems you have lost your Internet connection.
  > Try again later
* Button:

  * **Check connection**

---

## Global Interaction Rules (IMPORTANT)

Ці правила застосовуються **до всього додатку**, але критично важливі для Settings.

---

### UI / UX Rules

* Status bar та Home Indicator:

  * не перекривають контент;
  * мають власний background або blur при scroll.
* При тапі на будь-яку кнопку:

  * коротка анімація (scale / color change).
* Keyboard:

  * закривається тапом по вільній області.
* Context menu:

  * закривається тапом по вільній області.
* Sheet / Action Sheet / Share Sheet:

  * закриваються тапом по backdrop.
* Alert:

  * закривається **тільки** кнопками дій.

---

### Input & Validation

* Валідація полів:

  * працює при:

    * ручному введенні;
    * вставці тексту.
* Помилки показуються одразу.

---

### Haptics

Системний тактильний фідбек використовується для:

* toggles
* sliders
* pickers
* selection
* success / error
* biometric actions (Touch / Face ID)

---

### Navigation Rules

* **Back button не працює на Android** (жест / системна кнопка ігнорується).
* Welcome / Onboarding / Tips:

  * показуються **тільки при першому запуску**.

---

## Technical Notes

### Shared Logic File

* Логіка **Privacy Policy** та **Share App**:

  * повинна бути реалізована **в одному файлі**.


### Required File

* Створити файл:

```
settings-path.txt
```

#### Content:

```
lib/ui/settings_screen.dart
```

(або фактичний шлях до файлу з логікою Privacy Policy та Share App)

---
